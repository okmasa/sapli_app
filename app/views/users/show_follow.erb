<% provide(:title, @title) %>
<div class="container">
  <%# <div class="row">
    <aside class="col-md-4">
      <section class="user-img">
        <%= render 'shared/user_img' %>
      <%# </section>
      <section class="user_info">
        <%= render 'shared/user_info' %>
      <%# </section>
      <section class="stats">
        <%= render 'shared/stats' %>
      <%# </section>
    </aside> %>
      <h3><%= @user.name %>の<%= @title %></h3>
      <% if @users.any? %>
        <ul class="users follow">
          <%= render 'users/user' %>
        </ul>
        <div class="col-md-10">
          <%= will_paginate %>
        </div>
      <% end %>
    </div>
</div>