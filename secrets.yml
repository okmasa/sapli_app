development:
  host:              localhost # EC2インスタンス の パブリック DNS (IPv4) を入力する。
  access_key_id:     AKIATSJ4PMRBDFAKQPXJ
  secret_access_key:  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
  address:           email-smtp.us-west-2.amazonaws.com              # 自身が仕様しているリージョン
  domain:            sapliapli.com                                      # 上記で取得した値

production:
  host:              <%= ENV['AWS_HOST'] %>               # ElasticBeanstalk を使う場合、環境変数へ入力する。
  access_key_id:     <%= ENV['AWS_ACCESS_KEY_ID'] %>      # ElasticBeanstalk を使う場合、環境変数へ入力する。
  secret_access_key: <%= ENV['AWS_SECRET_ACCESS_KEY'] %>  # ElasticBeanstalk を使う場合、環境変数へ入力する。
  address:           <%= ENV['AWS_ADDRESS'] %>            # ElasticBeanstalk を使う場合、環境変数へ入力する。
  domain:            <%= ENV['AWS_DOMAIN'] %>             # ElasticBeanstalk を使う場合、